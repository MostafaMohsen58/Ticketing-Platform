@model Tixora.Models.Organizer

@{
    ViewData["Title"] = "Delete Organizer";
}

<h1 class="text-danger">Delete Organizer</h1>

<h4>Are you sure you want to delete this organizer?</h4>

<div class="card mt-4">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Name)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.ContactEmail)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.ContactEmail)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.ContactPhone)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.ContactPhone)</dd>

            @if (!string.IsNullOrEmpty(Model.LogoUrl))
            {
                <dt class="col-sm-3">Logo</dt>
                <dd class="col-sm-9">
                    <img src="/images/user/@Model.LogoUrl" alt="Logo" style="max-height: 80px; max-width: 100px;" />
                </dd>
            }
        </dl>

        @if (Model.Events != null && Model.Events.Any())
        {
            <div class="alert alert-warning">
                <h5>Warning!</h5>
                <p>This organizer is associated with @Model.Events.Count event(s). Deleting it might affect existing events.</p>
            </div>
        }

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</div>
