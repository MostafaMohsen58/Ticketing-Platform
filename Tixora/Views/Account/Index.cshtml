@model IEnumerable<EditProfileViewModel>

    @{
	Layout = "_AdminLayout";
    }

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users - Tixora</title>
    <!-- Bootstrap CSS -->

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        .users-section {
            padding: 2rem 0;
        }

        .users-table {
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
            border-radius: 10px;
            overflow: hidden;
        }

            .users-table thead {
                background-color: #f8f9fa;
                text-align:center;
            }

            .users-table th {
                font-weight: 600;
                text-transform: uppercase;
                font-size: 0.85rem;
                padding: 1rem;
                border-bottom: 2px solid #dee2e6;
            }

            .users-table td {
                padding: 1rem;
                vertical-align: middle;
            }

            .users-table tbody tr {
                transition: background-color 0.2s ease;
				text-align: center;
            }

                .users-table tbody tr:hover {
                    background-color: rgba(0, 123, 255, 0.05);
                }

        .user-profile-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

        .btn-action {
            min-width: 80px;
        }

        .header-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }

        .search-container {
            max-width: 300px;
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
        }
        
        /* Custom modal styling */
        .modal-header {
            background-color: #0e1b4d; 
            color: white; 
            padding: 8px 20px;
        }
        
        .modal-title {
            font-size: 16px;
        }
        
        .modal-body {
            background-color: #ffffff; 
            color: #0e1b4d; 
            font-weight: bold; 
            font-size: 16px; 
            padding: 20px;
        }
        
        .modal-footer {
            background-color: #ffffff; 
            padding: 8px 10px;
        }
        
        .btn-confirm-delete {
            background-color: #f82249; 
            color: white; 
            border: 2px solid #f82249; 
            border-radius: 5px; 
            padding: 8px 16px; 
            font-size: 16px;
        }
        
        .btn-cancel-delete {
            background-color: #ffffff; 
            color: #f82249; 
            border: 2px solid #f82249; 
            border-radius: 5px; 
            padding: 8px 16px; 
            font-size: 16px;
        }
    </style>
</head>
<body>
    @* <div class="container users-section">
        <div class="header-actions">
            <h1>Users Management</h1>
            <div class="d-flex gap-3 align-items-center">
                <form method="get" asp-action="Index" class="search-container d-flex">
                    <div class="input-group">
                        <input type="text" class="form-control" name="searchEmail" placeholder="Search users by email..." value="@Context.Request.Query["searchEmail"]">
                        <button class="btn btn-outline-primary" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                    <a href="@Url.Action("Index", "Account")" class="btn btn-outline-secondary ms-2">
                        <i class="bi bi-x-circle"></i> Clear
                    </a>
                </form>
                <a asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle me-2"></i>Add New User
                </a>
            </div>
        </div> *@
    <div class="container users-section">
        <div class="header-actions">
            <h1>Users Management</h1>
            <div class="d-flex flex-wrap gap-3 align-items-stretch">
                <form method="get" asp-action="Index" class="d-flex flex-grow-1 gap-2">
                    <div class="input-group flex-grow-1">
                        <input type="text" class="form-control" name="searchEmail" placeholder="Search users by email..." value="@Context.Request.Query["searchEmail"]">
                        <button class="btn btn-outline-primary" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                    <a href="@Url.Action("Index", "Account")" class="btn btn-outline-secondary d-flex align-items-center">
                        <i class="bi bi-x-circle me-1"></i> Clear
                    </a>
                </form>
                <a asp-action="Create" class="btn btn-primary d-flex align-items-center">
                    <i class="bi bi-plus-circle me-2"></i>Add New User
                </a>
            </div>
        </div>
    </div>


        @if (Model != null && !Model.Any())
        {
            <div class="alert alert-warning" role="alert">
                No users found.
            </div>
        }
        else
        {
        <div class="table-responsive users-table" style="margin: 8rem; margin-top: 0px; margin-bottom:0px">
                <table class="table table-borderless mb-3">
                    <thead>
                        <tr>
                            <th>Profile</th>
                            <th>Email</th>
                            <th>Address</th>
                            <th>City</th>
                            <th>Phone Number</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <img src="/images/user/@item.ProfileUrl" alt="@item.Email" class="user-profile-img" />
                                </td>
                                <td>
                                    <strong>@Html.DisplayFor(modelItem => item.Email)</strong>
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.City)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/Account/Edit?id=@item.Id" class="btn btn-primary btn-action">
                                            <i class="bi bi-pencil me-1"></i>Edit
                                        </a>
                                        <a href="/Account/Details?id=@item.Id" class="btn btn-warning btn-action">
                                            <i class="bi bi-eye me-1"></i>Details
                                        </a>
                                        <button type="button"
                                                class="btn btn-danger btn-action"
                                                data-bs-toggle="modal"
                                                data-bs-target="#deleteModal"
                                                data-id="@item.Id"
                                                data-email="@item.Email"
                                                data-address="@item.Address"
                                                data-city="@item.City"
                                                data-phone="@item.PhoneNumber">
                                            <i class="bi bi-trash me-1"></i>Delete
                                        </button>

                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        

        <div class="pagination-container">
            <nav>
                <ul class="pagination">
                    @if (ViewBag.CurrentPage > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" href="?searchEmail=@ViewBag.SearchEmail&pageNumber=@(ViewBag.CurrentPage - 1)">Previous</a>
                        </li>
                    }

                    @for (int i = 1; i <= ViewBag.TotalPages; i++)
                    {
                        <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                            <a class="page-link" href="?searchEmail=@ViewBag.SearchEmail&pageNumber=@i">@i</a>
                        </li>
                    }

                    @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" href="?searchEmail=@ViewBag.SearchEmail&pageNumber=@(ViewBag.CurrentPage + 1)">Next</a>
                        </li>
                    }
                </ul>
            </nav>

        </div>
    </div>
   
    <!-- Delete Confirmation Modal -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog" style="max-width: 500px;">
            <div class="modal-content" style="background-color: #ffffff;">
                <div class="modal-header" style="background-color: #0e1b4d; color: white; padding: 8px 20px;">
                    <h5 class="modal-title" id="deleteModalLabel" style="font-size: 16px;">Confirm Delete</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" style="background-color: #ffffff; color: #0e1b4d; font-weight: bold; font-size: 16px; padding: 20px;">
                    Are you sure you want to delete this user ?
                    <div class="modal-body">
                        <p><strong>Email:</strong> <span id="userEmailToDelete"></span></p>
                        <p><strong>Address:</strong> <span id="userAddressToDelete"></span></p>
                        <p><strong>City:</strong> <span id="userCityToDelete"></span></p>
                        <p><strong>Phone Number:</strong> <span id="userPhoneToDelete"></span></p>
                        <p>Are you sure you want to delete this user?</p>
                    </div>
                </div>
                <div class="modal-footer" style="background-color: #ffffff; padding: 8px 10px;">
                    <form id="deleteUserForm" action="/Account/ConfirmDelete" method="post">
                        <input type="hidden" name="id" id="userIdToDelete" />
                      
                        <button type="submit" class="btn" style="background-color:#f82249; color: white; border: 2px solid #f82249; border-radius: 5px; padding: 8px 16px; font-size: 16px;">Yes</button>
                      
                        <button type="button" class="btn" style="background-color: #ffffff; color: #f82249; border: 2px solid #f82249; border-radius: 5px; padding: 8px 16px; font-size: 16px;" data-bs-dismiss="modal">No</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap & jQuery Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // JavaScript for handling the delete confirmation modal
        $(document).ready(function() {
            // When the delete button is clicked, set the user ID and email in the modal
            $('#deleteModal').on('show.bs.modal', function (event) {
                var button = $(event.relatedTarget); // Button that triggered the modal
                
                var userId = button.data('id');
                var userEmail = button.data('email');
                var userAddress = button.data('address');
                var userCity = button.data('city');
                var userPhone = button.data('phone');

                // Update the modal's content
                var modal = $(this);
                modal.find('#userIdToDelete').val(userId);
                modal.find('#userEmailToDelete').text(userEmail);
                modal.find('#userAddressToDelete').text(userAddress);
                modal.find('#userCityToDelete').text(userCity);
                modal.find('#userPhoneToDelete').text(userPhone);
            });
        });
    </script>
</body>
</html>