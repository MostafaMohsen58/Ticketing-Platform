@model EditProfileViewModel

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Details";
}

@* <h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt class = "col-sm-2">
			<label>Image</label>
        </dt>
        <dd class = "col-sm-10">
            <img src="/images/user/@Model.ProfileUrl" alt="user image" width="100" height="100" />
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Address)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.City)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.City)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>
    </dl>
</div>
<div>
    <a href="/Account/Edit?id=@Model.Id">Edit</a>|

    <a asp-action="Index">Back to List</a>
</div> *@

@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users - Tixora</title>
    <style>

        .user-card {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            margin-bottom: 2rem; /* Increased */
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .user-img-container {
            width: 120px; /* Increased from 80px */
            height: 120px; /* Increased from 80px */
            overflow: hidden;
            border-radius: 50%;
            margin: 0 auto;
        }

        .card-body {
            padding: 2rem 1.5rem; /* Added padding */
        }

        .card-title {
            font-weight: 600;
            margin-bottom: 0.5rem;
            font-size: 1.25rem; /* Increased */
        }

        .user-info {
            display: flex;
            flex-direction: column;
            gap: 0.75rem; /* Increased from 0.5rem */
            font-size: 1rem; /* Added font size */
            margin-bottom: 1.5rem; /* Increased from 0.3rem */
        }

        .user-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.12);
        }

        .user-img-container {
            width: 80px;
            height: 80px;
            overflow: hidden;
            border-radius: 50%;
            margin: 0 auto;
        }

        .user-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }


        .action-buttons {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

        .user-info-item {
            display: flex;
        }

        .user-info-label {
            font-weight: 500;
            min-width: 100px;
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
        }

        .search-container {
            margin-bottom: 2rem;
        }


    </style>
</head>
<body>
    <div class="container user-table-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">Users Management</h1>
        </div>

        

        <!-- Card View for Larger Screens -->
        <div class="row d-none d-lg-flex">
            
                <div class="col-lg-6 col-xl-4">
                    <div class="card user-card">
                        <div class="card-body text-center">
                            <div class="user-img-container mb-3">
                            <img src="/images/user/@Model.ProfileUrl" alt="@Model.Email" class="user-img">
                            </div>
                        <h5 class="card-title">@Model.Email</h5>
                            <div class="user-info mb-3">
                                <div class="user-info-item">
                                    <span class="user-info-label">Address:</span>
                                <span>@Model.Address</span>
                                </div>
                                <div class="user-info-item">
                                    <span class="user-info-label">City:</span>
                                <span>@Model.City</span>
                                </div>
                                <div class="user-info-item">
                                    <span class="user-info-label">Phone:</span>
                                <span>@Model.PhoneNumber</span>
                                </div>
                            </div>
                            <div class="action-buttons">
                            <a href="/Account/Edit?id=@Model.Id" class="btn btn-sm btn-primary">
                                    <i class="bi bi-pencil me-1"></i>Edit
                                </a>
                            <a href="/Account/Details?id=@Model.Id" class="btn btn-sm btn-warning">
                                    <i class="bi bi-eye me-1"></i>Details
                                </a>
                            <a href="/Account/Delete?id=@Model.Id" class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash me-1"></i>Delete
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            
        </div>


       
    </div>

    <script>
        // Optional JavaScript for enhanced functionality
        document.addEventListener('DOMContentLoaded', function() {
            // You could add functionality here like:
            // - Implementing the search feature
            // - Adding confirmation dialogs for delete actions
            // - Handling pagination dynamically
        });
    </script>
</body>
</html>
 *@
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Details - Tixora</title>
    <style>
        .details-container {
            padding: 3rem 0;
            max-width: 800px;
            margin: 0 auto;
        }

        .details-card {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }

        .user-img-container {
            width: 180px;
            height: 180px;
            overflow: hidden;
            border-radius: 50%;
            margin: 0 auto;
            border: 5px solid #fff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .user-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card-body {
            padding: 2.5rem;
        }

        .card-title {
            font-weight: 700;
            font-size: 1.75rem;
            margin-bottom: 1.5rem;
            color: #333;
        }

        .user-info {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            font-size: 1.1rem;
            margin-bottom: 2rem;
        }

        .user-info-item {
            display: flex;
            border-bottom: 1px solid #eee;
            padding-bottom: 0.75rem;
        }

        .user-info-label {
            font-weight: 600;
            min-width: 120px;
            color: #555;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 1.5rem;
        }

        .btn-back {
            min-width: 120px;
        }
    </style>
</head>
<body>
    <div class="container details-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">User Details</h1>
            <a href="/Account/Index" class="btn btn-outline-primary btn-back">
                <i class="bi bi-arrow-left me-2"></i>Back to List
            </a>
        </div>

        <div class="card details-card">
            <div class="card-body">
                <div class="text-center mb-4">
                    <div class="user-img-container mb-4">
                        <img src="/images/user/@Model.ProfileUrl" alt="@Model.Email" class="user-img">
                    </div>
                    <h3 class="card-title">@Model.Email</h3>
                </div>

                <div class="user-info">
                    <div class="user-info-item">
                        <span class="user-info-label">Address:</span>
                        <span>@Model.Address</span>
                    </div>
                    <div class="user-info-item">
                        <span class="user-info-label">City:</span>
                        <span>@Model.City</span>
                    </div>
                    <div class="user-info-item">
                        <span class="user-info-label">Phone:</span>
                        <span>@Model.PhoneNumber</span>
                    </div>
                    <!-- Add any additional user details here -->
                </div>

                <div class="action-buttons">
                    <a href="/Account/Edit?id=@Model.Id" class="btn btn-primary">
                        <i class="bi bi-pencil me-2"></i>Edit Profile
                    </a>
                    <a href="/Account/Delete?id=@Model.Id" class="btn btn-danger">
                        <i class="bi bi-trash me-2"></i>Delete User
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>